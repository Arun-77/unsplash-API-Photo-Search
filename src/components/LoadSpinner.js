import React from 'react';
import classes from './Spinner.module.css';

const LoadSpinner = () => {
	return <div style={{ marginTop: '2rem' }} className={classes.loader}></div>;
};

export default LoadSpinner;
